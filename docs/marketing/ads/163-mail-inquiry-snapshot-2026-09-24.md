# 163 邮箱询盘候选快照（2026-09-24）

> 邮箱类型：普通 `@163.com`
> 采集日期：2026-09-24
> 数据窗口：2026-08-25 至 2026-09-23（最近 30 个完整自然日）
> 对照窗口：2026-07-26 至 2026-08-24（此前 30 个完整自然日）
> 报告时区：`America/New_York`
> 数据范围：只读 `INBOX + Junk/Spam + Trash`；不含 Sent、自定义文件夹和附件
> 状态：完整文件夹范围快照已完成；询盘真伪、质量与销售阶段人工确认待补

本文件最初只读取 `INBOX`，得到最近窗口 8 封邮件与 5 封规则候选。所有者确认 Junk 中仍有
询盘相关邮件后，同日完成完整重跑；当前下列数据已经取代首轮 `INBOX` 单独结果。

本快照使用与 Google Ads API 快照相同的两个完整 30 天窗口。连接采用 IMAP SSL、Coremail
RFC 2971 `ID` 握手、只读 `EXAMINE` 和 `BODY.PEEK[]`，自动发现服务器 SPECIAL-USE 标记的
Junk 和 Trash，并按 Message-ID 跨文件夹去重。三个目标角色全部成功读取，缺失角色 0，解析
失败 0；没有发送、删除、移动邮件或改变已读状态。原始正文、主题、完整发件地址和附件未保存，
也未进入 Git。

## 1. 窗口结果

| 指标 | 此前 30 天 | 最近 30 天 |
|---|---:|---:|
| 三个目标文件夹去重后邮件 | 0 | 20 |
| `INBOX` 邮件 | 0 | 8 |
| Junk/Spam 邮件 | 0 | 12 |
| Trash 邮件 | 0 | 0 |
| 询盘候选（规则识别，未人工确认） | 0 | 10 |
| 其他邮件 | 0 | 7 |
| 推销/垃圾候选 | 0 | 1 |
| 需要人工复核 | 0 | 12 |
| 候选中明确出现达到 MOQ 的数量 | 0 | 1 |
| 候选中明确出现低于 MOQ 的数量 | 0 | 1 |
| 询盘候选 MOQ 未知 | 0 | 8 |
| 明确自述来自 Google | 0 | 0 |
| 询盘候选来源未知 | 0 | 10 |
| 无法解析或读取 | 0 | 0 |

最近窗口的 20 封邮件中，`INBOX` 与 Junk 各有 5 封询盘候选；如果只读 `INBOX`，会遗漏
一半候选。另有 2 封 Junk 邮件处于 `needs_review`，未计入 10 封候选。产品词识别到
Sportswear 5 封、Outdoor Clothing 3 封、Underwear 1 封、Merino Wool 1 封；标签不是互斥
分类，同一邮件可能出现多个产品词。正文共有 4 封出现 China，但这只代表词语出现，不证明
客户国家、收货国家或来源地区。

候选中一封明确出现数量 500，另一封出现数量 1，其余八封未提取到明确数量。数量 1 可能是
样品数量，不足以直接判定为低于 MOQ 的正式订单；数量 500 也不证明是每款数量或最终订单量。

## 2. 不能从本次结果得出的结论

- 不能把十封候选全部写成真实或合格询盘；它们仍需人工确认。
- 不能把十封候选归因给 Google Ads。没有邮件明确自述来自 Google，来源保持 `unknown`。
- 不能把“此前窗口三个目标文件夹为 0”写成此前 30 天没有询盘；邮件可能已被永久删除或移至
  未读取的自定义文件夹。
- 不能根据没有数量词就判断低于或高于 MOQ；八封候选的 MOQ 状态为 `unknown`，另外两封只
  记录明确出现的数量，不自动解释为最终采购量。
- 不能根据正文出现国家名推断客户所在地。

## 3. P0 处置

- Google Ads 平台快照与邮箱三个目标文件夹的聚合快照均已完成。
- P0 继续保持 `进行中`，因为十封候选和两封 `needs_review` 尚未完成人工真伪、需求质量和
  销售阶段确认。
- 不增加预算、不改出价、不添加否定关键词，也不把邮件候选数量写成 Google Ads conversion。
- 新授权码由所有者在本机输入，并由 Windows DPAPI 按当前用户加密；配置和报告均保存在
  Git 仓库外。尚未创建每日计划任务，因此当前状态为 `connected / manual-run`，不是自动日更。
