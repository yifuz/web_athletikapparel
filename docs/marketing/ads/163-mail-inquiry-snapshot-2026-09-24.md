# 163 邮箱询盘候选快照（2026-09-24）

> 邮箱类型：普通 `@163.com`
> 采集日期：2026-09-24
> 数据窗口：2026-08-25 至 2026-09-23（最近 30 个完整自然日）
> 对照窗口：2026-07-26 至 2026-08-24（此前 30 个完整自然日）
> 报告时区：`America/New_York`
> 数据范围：只读 `INBOX`；不含 Sent、Spam、Trash、自定义文件夹和附件
> 状态：**不完整 / 不用于 P0 总量结论**；所有者随后确认 Junk/Trash 中存在邮件

> 2026-09-24 状态更正：本次快照只读取 `INBOX`。所有者随后确认垃圾邮件或回收站中仍有
> 询盘相关邮件，因此下列数字只保留为连接验证记录，不能作为邮箱询盘总量。完整重跑必须覆盖
> `INBOX + Junk/Spam + Trash`，并按 Message-ID 去重。

本快照使用与 Google Ads API 快照相同的两个完整 30 天窗口。连接采用 IMAP SSL、Coremail
RFC 2971 `ID` 握手、只读 `EXAMINE` 和 `BODY.PEEK[]`，没有发送、删除、移动邮件或改变
已读状态。原始正文、主题、完整发件地址和附件未保存，也未进入 Git。

## 1. 窗口结果

| 指标 | 此前 30 天 | 最近 30 天 |
|---|---:|---:|
| `INBOX` 收到邮件 | 0 | 8 |
| 询盘候选（规则识别，未人工确认） | 0 | 5 |
| 其他邮件 | 0 | 3 |
| 推销/垃圾候选 | 0 | 0 |
| 需要人工复核 | 0 | 5 |
| 邮件明确数量达到 MOQ | 0 | 0 |
| 邮件明确数量低于 MOQ | 0 | 0 |
| 询盘候选 MOQ 未知 | 0 | 5 |
| 明确自述来自 Google | 0 | 0 |
| 询盘候选来源未知 | 0 | 5 |
| 无法解析或读取 | 0 | 0 |

最近窗口的五封候选邮件中，规则识别到 Sportswear 2 封、Underwear 1 封、Outdoor Clothing
1 封，另 1 封没有稳定产品分类。正文出现一次 China，但这只代表词语出现，不证明客户国家、
收货国家或来源地区。

## 2. 不能从本次结果得出的结论

- 不能把五封候选全部写成真实或合格询盘；它们仍需人工确认。
- 不能把五封候选归因给 Google Ads。没有邮件明确自述来自 Google，来源保持 `unknown`。
- 不能把“此前窗口 `INBOX` 为 0”写成此前 30 天没有询盘；邮件可能已被归档、删除或移至其他文件夹。
- 不能根据没有数量词就判断低于或高于 MOQ；五封候选的 MOQ 状态均为 `unknown`。
- 不能根据正文出现国家名推断客户所在地。

## 3. P0 处置（状态更正）

- Google Ads 平台快照已完成；邮箱快照未完成，因为首轮遗漏 Junk/Spam 和 Trash。
- P0 继续保持 `进行中`；不能使用五封 `INBOX` 候选作为全部邮件询盘分母或总量。
- 不增加预算、不改出价、不添加否定关键词，也不把邮件候选数量写成 Google Ads conversion。
- 所有者只授权本次使用客户端授权码；采集完成后，本机 `config.json` 和 DPAPI 加密授权码文件
  已删除，未创建计划任务。聚合报告继续保存在 Git 仓库外。

分析器已调整为自动发现服务器 SPECIAL-USE 标记的 Junk/Spam 和 Trash，并与 `INBOX` 跨文件夹
去重。完整重跑需要重新生成客户端授权码并再次执行本机配置；在此之前工具处于
`implemented / disconnected / rerun-required`，不是持续同步状态。
