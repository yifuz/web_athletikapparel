# 163 邮箱询盘确认快照（2026-09-24）

> 邮箱类型：普通 `@163.com`
> 采集与复核日期：2026-09-24
> 数据窗口：2026-08-25 至 2026-09-23（最近 30 个完整自然日）
> 对照窗口：2026-07-26 至 2026-08-24（此前 30 个完整自然日）
> 报告时区：`America/New_York`
> 数据范围：只读 `INBOX + Junk/Spam + Trash`；不含 Sent、自定义文件夹和附件
> 状态：完整文件夹范围快照、规则校准和邮件正文人工确认已完成

本文件最初只读取 `INBOX`，得到最近窗口 8 封邮件与 5 封规则候选。所有者确认 Junk 中仍有
询盘相关邮件后，同日完成完整重跑；随后对 20 封邮件逐封进行内容级确认，并根据已确认结果
校准分类规则。当前结论取代首轮 `INBOX` 单独结果和校准前的 10 封规则候选结果。

本快照使用与 Google Ads API 快照相同的两个完整 30 天窗口。连接采用 IMAP SSL、Coremail
RFC 2971 `ID` 握手、只读 `EXAMINE` 和 `BODY.PEEK[]`，自动发现服务器 SPECIAL-USE 标记的
Junk 和 Trash，并按 Message-ID 跨文件夹去重。三个目标角色全部成功读取，缺失角色 0，解析
失败 0；没有发送、删除、移动邮件或改变已读状态。原始正文、主题、完整发件地址和附件未保存，
也未进入 Git。人工复核台账仅保存在本机私有目录，Git 文档不包含客户身份或联系方式。

## 1. 窗口结果

| 指标 | 此前 30 天 | 最近 30 天 |
|---|---:|---:|
| 三个目标文件夹去重后邮件 | 0 | 20 |
| `INBOX` 邮件 | 0 | 8 |
| Junk/Spam 邮件 | 0 | 12 |
| Trash 邮件 | 0 | 0 |
| 规则识别询盘候选 | 0 | 5 |
| 人工确认真实询盘 | 0 | 5 |
| 人工确认非询盘 | 0 | 15 |
| 真实询盘位于 `INBOX` | 0 | 4 |
| 真实询盘位于 Junk/Spam | 0 | 1 |
| 明确自述来自 Google | 0 | 0 |
| 自述来自一般网络渠道、未指明 Google | 0 | 1 |
| 来源未知的真实询盘 | 0 | 4 |
| 无法解析或读取 | 0 | 0 |

校准后的规则结果为 5 封 `inquiry_candidate`、10 封 `vendor_or_spam` 和 5 封 `other`；五封
候选全部需要人工销售判断，并均被确认是真实买家询盘。其余 15 封经逐封复核为网站/营销服务、
展会服务、供应商推销、名单销售、订阅邮件、测试或其他无关内容，没有发现被规则漏掉的真实询盘。

Junk/Spam 的 12 封邮件中只有 1 封是真实询盘。它证明垃圾箱必须读取，但也证明不能直接把
垃圾箱中的高关键词邮件计为线索。对照窗口三个目标文件夹均为 0；这只是当前邮箱状态，不证明
当时没有询盘，因为邮件可能被永久删除或移到未读取的自定义文件夹。

## 2. 询盘质量初步确认

五封真实询盘的数量信号为：

- 1 封提出总量约 1,500–2,500 件、分布在 2–3 个设计中，可能达到 MOQ；但每款数量未确认，
  因此不能直接记为合格 MOQ 询盘。
- 1 封询问 50 / 100 / 250 / 500 件的价格情景，500 是询价档位而不是采购承诺；该询盘存在
  初单低于 MOQ 的明显风险。
- 其余 3 封没有可确认数量，其中 1 封的规格位于未读取的 PDF 附件中。

五封询盘中，1 封明确自述通过一般网络渠道找到联系方式，但没有说明 Google、Google Ads、
自然搜索或其他具体来源；其余 4 封没有来源陈述。因而 5 封均不能归因给 Google Ads。

本轮只完成真伪、初步需求质量、数量与自述来源确认。由于未读取 Sent，也没有销售台账，是否
回复、报价、打样、成交或流失仍为 `unavailable`。附件未读取，因此附件内可能存在的数量、
规格或商业条件仍是证据缺口。

## 3. 不能从本次结果得出的结论

- 不能把五封真实询盘写成五封合格询盘；其中只有一封表现出潜在 MOQ 匹配，且仍需确认每款数量。
- 不能把任何一封询盘归因给 Google Ads；邮件时间接近广告点击不构成归因证据。
- 不能把一般网络来源写成 Google，也不能把四封未知来源按比例分配给付费或自然搜索。
- 不能把对照窗口的 0 写成此前 30 天没有询盘。
- 不能从未读附件或未读 Sent 推断采购数量、回复状态或销售阶段。

## 4. P0 处置

- Google Ads 平台快照与邮箱三个目标文件夹的聚合快照均已完成。
- 20 封邮件已完成内容级初步核对：5 封真实询盘、15 封非询盘；来源、数量和证据缺口已明确记录。
- P0 完成门槛已满足，转为 `已完成`。来源无法确认是已核验的证据结果，不再作为无限期阻塞项。
- 下一节点为 P1：使用本机私有台账持续记录新询盘、负责人、MOQ、来源与销售阶段；未确认字段
  继续写 `unknown` 或 `unavailable`。
- 不增加预算、不改出价、不添加否定关键词，也不把五封邮件询盘写成 Google Ads conversion。
- 授权码由所有者在本机输入，并由 Windows DPAPI 按当前用户加密；配置和报告均保存在
  Git 仓库外。尚未创建每日计划任务，因此当前状态为 `connected / manual-run`，不是自动日更。
